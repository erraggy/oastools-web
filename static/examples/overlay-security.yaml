overlay: "1.0.0"
info:
  title: Add Security
  version: "1.0.0"
actions:
  - target: $.components
    update:
      securitySchemes:
        ApiKeyAuth:
          type: apiKey
          in: header
          name: X-API-Key
  - target: $
    update:
      security:
        - ApiKeyAuth: []
